# Configuration de stockage des justificatifs

# Mode de stockage: 'local' ou 'cloud'
STORAGE_MODE=local

# Configuration stockage local
LOCAL_STORAGE_PATH=./storage/justificatifs

# Configuration stockage cloud (S3-compatible)
# Décommenter et remplir pour utiliser le cloud
# S3_ACCESS_KEY_ID=your_access_key
# S3_SECRET_ACCESS_KEY=your_secret_key
# S3_REGION=eu-west-1
# S3_BUCKET=compta-justificatifs

# Pour MinIO ou autres services compatibles S3
# S3_ENDPOINT=http://localhost:9000
# S3_FORCE_PATH_STYLE=true
